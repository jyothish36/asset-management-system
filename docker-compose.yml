services:

  backend:
    build: ./backend
    container_name: asset_backend

    ports:
      - "8000:8000"

  frontend:
    build: ./frontend/asset-management-ui
    container_name: asset_frontend

    ports:
      - "3000:3000"

    depends_on:
      - backend